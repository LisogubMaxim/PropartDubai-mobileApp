import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    gap: 12,
    marginBottom: 26,
  },

  row: {
    gap: 12,
    flexDirection: 'row',
  },

  doubleButton: {
    width: '48.2%',
  },

  pass: {
    flex: 1,
  },
});

export default styles;
